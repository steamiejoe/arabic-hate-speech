# ==============================================================================
# STANDARD PYTHON & SYSTEM FILES
# ==============================================================================

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Logs
*.log

# Sphinx / Documentation
docs/_build/

# Mac OS
.DS_Store

# ==============================================================================
# IDE & TOOLS
# ==============================================================================

# Jupyter Notebook
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# VS Code
.vscode/

# PyCharm / JetBrains
.idea/

# Spyder
.spyderproject
.spyproject

# Rope
.ropeproject

# mypy
.mypy_cache/

# Ruff
.ruff_cache/

# Streamlit
# Secrets should never be committed
.streamlit/secrets.toml
# Streamlit cache (optional, but good to ignore)
.streamlit/
# Exception: Keep config if you have one
!.streamlit/config.toml

# ==============================================================================
# PROJECT SPECIFIC: DATA & MODELS (CRITICAL)
# ==============================================================================

# --- 1. Data Folders (Keep structure, ignore content) ---
# Ignore everything in these folders...
data/raw/*
data/interim/*
data/processed/*

# ...EXCEPT the .gitkeep files (so the empty folders exist on GitHub)
!data/raw/.gitkeep
!data/interim/.gitkeep
!data/processed/.gitkeep

# --- 2. Model Weights (Too large for GitHub) ---
# PyTorch weights (often 100MB+)
*.pth
*.pt
*.bin
*.ckpt
*.h5

# Pickle files (often contain serialized data)
*.pkl

# HuggingFace / Transformers cache (if saved locally in a models folder)
models/*
!models/.gitkeep

# --- 3. Data File Extensions (Global Ignore) ---
# Ignore these extensions anywhere in the project to prevent accidental leaks
*.csv
*.tsv
*.xlsx
*.xls
*.parquet
*.jsonl
*.txt

# --- 4. EXCEPTIONS (Files we MUST track) ---
# Since we ignored *.txt above, we must force Git to see these:
!requirements.txt
!README.md
!DATASET_CARD.md
!LICENSE